class User {
    string name
    int age

    fn say_hello(User& user) {
        print("Hello, I am " + user.name + " and I am " + user.age.to_string() + " years old!")
    }
}


fn main() -> int {
    john = new User("John Smith", 32)
    john.say_hello()
    return 0
}
